npm install  # ✅ Sudah dijalankan dan berhasil
npm start    # ✅ Server berhasil berjalan di port 4000# Server Configuration
PORT=4000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_ANON_KEY=<your-anon-key>
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# Instructions:
# 1. Copy this file to .env
# 2. Replace <your-project-ref> with your actual Supabase project reference
# 3. Replace <your-anon-key> with your actual Supabase anonymous key
# 4. Replace <your-service-role-key> with your actual Supabase service role key
# 5. Never commit .env to version control (already in .gitignore)

# Security Notes:
# - SUPABASE_ANON_KEY: Safe to use in frontend applications
# - SUPABASE_SERVICE_ROLE_KEY: Keep secret! Has admin privileges, use only server-side
# - Service role key bypasses RLS, use with extreme caution